apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: cleanup

spec:
  description: Clean workspace by deleting all files
  workspaces:
    - name: source

  steps:
    - name: remove
      image: alpine:3
      workingDir: $(workspaces.source.path)

      env:
        - name: WORKSPACE_SOURCE_PATH
          value: $(workspaces.source.path)

      securityContext:
        runAsNonRoot: false
        runAsUser: 0

      script: |
        #!/usr/bin/env sh
        set -eu

        echo "Cleaning workspace: ${WORKSPACE_SOURCE_PATH}"

        if [ -d "${WORKSPACE_SOURCE_PATH}" ]; then
          rm -rf "${WORKSPACE_SOURCE_PATH:?}/"*
        fi
---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: pytest

spec:
  description: Run unit tests using pytest

  workspaces:
    - name: source

  steps:
    - name: test
      image: python:3.9-slim
      workingDir: $(workspaces.source.path)

      env:
        - name: PYTHONPATH
          value: $(workspaces.source.path)

      script: |
        pip install -r requirements.txt
        pytest -v
